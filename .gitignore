.DS_Store
*.skill
node_modules/
.opencode
# openspec

# Local-only working notes
SCRATCH.md
SCRATCHPAD.md

# Eval run outputs (regenerable; definitions live in evals/<skill>/)
evals/*/runs/
skills/*-workspace/

skills-lock.json
.agents

# Claude Code: commit the shared hook config + scripts; keep installed skills,
# the commands cache, and personal overrides (settings.local.json) out.
.claude/*
!.claude/settings.json
!.claude/hooks/